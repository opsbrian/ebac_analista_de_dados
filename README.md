# 🧮 Calculadora Interativa em Python (V1.0)

Este projeto é uma aplicação de terminal simples que realiza operações matemáticas básicas.

## 🚀 Funcionalidades

* **Operações Matemáticas**: Soma, Subtração, Multiplicação e Divisão.
* **Tratamento de Erros**: Uso de blocos `try...except` para evitar que o programa trave ao receber entradas não numéricas. 
* **Validação de Menu**: Garantia de que o usuário escolha apenas opções válidas (1 a 5).
* **Loop Contínuo**: O programa permite realizar múltiplos cálculos sem precisar ser reiniciado.

## 🛠️ Tecnologias Utilizadas

* **Linguagem**: Python 3.14
* **Conceitos**: 
    * Laços de repetição (`while`).
    * Estruturas condicionais (`if`, `elif`, `else`).
    * Captura de exceções (`ValueError`).
    * Desempacotamento de listas (`print(*opc)`).

## 📋 Como Executar

1.  Certifique-se de ter o Python instalado.
2.  Salve o código em um arquivo chamado `calculadora.py`.
3.  Abra o seu terminal.
4.  Execute o comando:
    
    python3 calculadora.py
    

## 📖 Estrutura do Código

O código segue o seguinte fluxo lógico:
1.  **Entrada de Dados**: Solicita dois números com validação `float`.
2.  **Exibição de Menu**: Mostra as opções de operação.
3.  **Processamento**: Executa a operação escolhida através de condicionais.
4.  **Encerramento**: Opção dedicada para sair do loop e fechar o programa.

---------------------------------------------------------------------------
Desenvolvido durante o curso de Analista de Dados pela Ebac.
